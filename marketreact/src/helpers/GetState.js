export  const userSerivce = (state) => state.mainData.userServiceModel;

export  const productSerivce = (state) => state.productsPageData.productServiceModel;

export const productModule = (state) => state.productsPageData.productsModule;

export const productTypeService = (state)  => state.headerPageData.productTypeServiceModel;