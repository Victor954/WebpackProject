export  const userSerivce = (state) => state.mainData.userServiceModel;

export  const productSerivce = (state) => state.homePageData.productServiceModel;

export const productModule = (state) => state.homePageData.productsModule;